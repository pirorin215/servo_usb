# servo_usb プロジェクト - エージェントへの指示

## シリアルポート・書込

- **固定ポート: `/dev/cu.usbmodemHIDEF1`** — 本プロジェクト専用・9600 baud 固定
  （他のボーレートでは無音）
- **本プロジェクトの開発書込はエージェントが実行してよい**（2026-09-10 ユーザー指示。
  常設の専用固定ポートのため書込先の混乱がなく、失敗してもリトライで無害）:
  1. `bash compile.sh` が成功していること・`/dev/cu.usbmodemHIDEF1` が存在することを確認
  2. `sh upload.sh` を実行する
  3. 失敗したら最大2回リトライ → それでも失敗ならユーザーへ引き継ぎ
  4. 書込後は dump や起動ログで新ファームの動作を確認してから次の作業へ進む
- **他プロジェクトの実験・テスト目的でこのポートへ書込まない**: 実験・ファーム
  検証は開発共通ポート `/dev/cu.usbmodem212101` か別ボードで行うこと
- 9600 baud のシリアルコマンド送信（`LEARN_*` / `DUMP_PATTERNS` / `SEND_*`）は
  書込ではないためエージェントから自由に実行してよい

## 自動ビルドルール（必須）

**重要:** Arduinoコード（`.ino`, `.cpp`, `.h`ファイル）を変更した場合、**必ず直後にビルドを実行すること。** コードを変更しただけではマイコンに書き込まれない。

### 手順

1. コードを変更する
2. **即座にビルドを実行**: `bash compile.sh`
3. ビルド結果をユーザーに報告する（成功・失敗問わず）

### ビルド結果の報告形式

**成功時:**
- ✅ ビルド成功
- Flash使用量 / RAM使用量を表示

**失敗時:**
- ❌ ビルド失敗
- エラーメッセージを表示
- 解決策を提示して修正

## ライブラリ依存に関する注意

- **IRremote は必ず v3.9.0 を使用**（`arduino-cli lib install IRremote@3.9.0`）。v4.x はAPI互換性がなくビルドエラーになる。詳細は [README.md](README.md) を参照。
- **バージョンガード付き**: `servo_usb.ino` に `#if VERSION_IRREMOTE_MAJOR != 3` のガードを仕込んでおり、v4.x が混入した場合はコンパイル時に `#error` で即座に停止する。Library Manager で誤って更新された場合はこのエラーで気付ける。
