# servo_usb 照明自動化 todo

> 目標: **Pixel 10 の家 Wi-Fi 接続/切断エッジで照明を自動 ON/OFF**（帰宅 ON・外出 OFF・
> 家の中では不反応・深夜誤爆なし）。「腕」は servo_usb の IR 送信（新規工作ゼロ）。
> GPSジオフェンスは使わない——tapo/SwitchBot のジオフェンス不発は Android の
> ジオフェンス配信機構の宿命で、Wi-Fi 接続イベント方式なら原理的に回避できる。

## 残課題

### フェーズ0: 事前確認（最初の関門・ユーザー作業含む）

- [x] 照明リモコンのメーカー確認——日立・東芝・三菱等のNEC系と回答（2026-09-10）。
  実機でもNECとして学習成功（TOSHIBA NLER005-LD・東芝LEDシーリング用）
- [x] servo_usb の現在の設置場所と、IR LED（D3）から照明までの見通し確認
  （2026-09-10 実機テストで点灯/消灯とも成功 = 見通し・距離とも問題なし）
- [x] 既存学習スロット確認——EEPROMダンプで ON/OFF/PLAYPAUSE すべてアドレス0x01
  （README記載のスマートリモコン0x82ではない別リモコン）。受信側（リモコン→サーボ）
  は現役・送信LED側は未使用と回答 → **スロット追加拡張で解決**（照明は独立スロットへ）
- [ ] 照明に ON/OFF 以外に自動化したい IR コマンドがあるか（常夜灯・調光等）確認
  （参考: 本リモコンには非NECボタンも混在——学習試行で Protocol:1
  PULSE_DISTANCE・Addr 0x25 のボタンを誤押下時に検出。現ファームはNECのみ学習可）

### フェーズ1: servo_usb ファーム拡張（SEND コマンド追加）

- [x] `SEND_LIGHT_ON` / `SEND_LIGHT_OFF` シリアルコマンド追加（2026-09-10）。
  当初予定の SEND_ON/SEND_OFF から変更: 既存スロットが受信側で現役のため照明は
  独立スロットに分離し、シナリオ（サーボ動作）を経由しない単発 IR 送信にした
- [x] 照明用パターンスロット追加（2026-09-10）: LIGHT_ON/LIGHT_OFF の2スロットを
  EEPROM末尾（436〜724バイト目・ATmega32U4の1KB内に収まる）へ追加。マジック
  IRL5→IRL6 へバンプし初回起動時に新領域だけ初期化（既存3スロットのアドレスは
  不変のため学習データはそのまま）。受信マッチング（SCENARIOS）には非登録のため
  照明リモコンを押してもサーボは動かない
- [x] `bash compile.sh` — 成功: Flash 23312バイト(81%) / RAM 1141バイト(44%)（2026-09-10）
- [x] `ir_learn.sh` に `light_on` / `light_off` モード追加、`light_on.sh` /
  `light_off.sh` 新設（フェーズ2の前倒し・UTF-8/構文検証済み）
- [x] 書込: `sh upload.sh` 実行・成功（2026-09-10）。dump で5スロット確認——
  既存3スロットの学習データは保持、LIGHT_ON/OFF は未学習の空スロットとして出現
- [x] 学習の受付窓を60秒へ拡張（ir_learn.sh・環境変数 IR_LEARN_WAIT で調整可）。
  旧実装は15ループ×0.3秒で実質5秒しかなくリモコンを構える余裕がなかった
- [x] 照明リモコンの学習（2026-09-10 ユーザー実施・成功）:
  LIGHT_ON=Addr 0xE7 Cmd 0xA2 / LIGHT_OFF=Addr 0xE7 Cmd 0x8B・いずれも
  Protocol:7 (NEC) len=67。Cmd が別 = **1ボタントグルではない独立コードと確定**
  （ユーザー回答でも ON/OFF 別ボタンと確認済み）。dump で保存確認済み。
  なおリモコンには非NECボタン（Protocol:1 PULSE_DISTANCE・Addr 0x25）も
  混在（最初の学習試行で誤押下→NECガードが正しく弾いた）
- [x] 学習中のノイズ耐性をファームに追加（2026-09-10）: Pattern length error でも
  学習モード継続、UNKNOWN+sigLen<30 の未成立フレーム（リピート相当）も
  スキップして待機継続。Flash 23442B=81% / RAM 1141B=44%
- [x] 実機テスト（2026-09-10 合格）: `./light_on.sh` / `./light_off.sh` で
  点灯/消灯とも成功。**送信50倍速バグを修正**（後述）してから動作
- [x] **送信単位バグ修正**（2026-09-10）: EEPROM保存値は受信TICKS（50μs）単位
  なのに `sendRaw(uint16_t版)` はマイクロ秒単位を期待し、50倍速い信号
  （リーダー9ms→180μs）を送っていた。送信時に×MICROS_PER_TICK変換を追加で解決。
  送信機能は長年未検証のまま（受信側のみ現役）だったため顕在化していなかった。
  シナリオ送信（CMD_SEND_IR経由）も同じ sendIRPattern 経由で同時に修正された。
  Flash 23476B=81% / RAM 1141B=44%

- [x] **自己受信ループ修正**（2026-09-10 夜発見・同日解決）: 送信機能が正常化した
  ことで、シナリオ内 CMD_SEND_IR の送信LEDの光が自受信機へ回り込み→自己受信→
  シナリオ再発動→また送信…の無限ループが既存リモコン操作（サーボOFF等）で
  発生（LEDを隠すと止まる）。`sendIRPattern()` を
  **IrReceiver.stop() → sendRaw → 10ms → IrReceiver.start()（バッファクリア）**
  にして自己受信を構造的に防止。照明送信・既存リモコン操作とも正常共存を
  実機確認済み。Flash 23576B=82% / RAM 1141B=44%

### フェーズ2: Mac側 送信スクリプト

- [x] `light_on.sh` / `light_off.sh` 新設（2026-09-10・フェーズ1と同時に前倒し）——
  HIDEF1（9600baud 固定）へ `SEND_LIGHT_ON\n` / `SEND_LIGHT_OFF\n` を書くだけ。
  ポートを掴むのは書き込みの瞬間のみ（他ツールとの排他・詳細は AGENTS.md）
- [x] ターミナルから実行して照明が点く/消えることを確認（2026-09-10 合格。
  音声「OK Google」の tapo 経路はそのまま残る・二重経路でも問題なし）

### フェーズ3: Wi-Fiエッジ判定（脳）— 2026-09-10 完成・**Tasker不採用のping監視方式に確定**

- [x] Mac側 受付API（2026-09-10）: `~/www/light-api/` 新設（独立Git f044862・
  `https://192.168.0.222/light-api/`）。POST `event=connected` → `light_on.sh` 即時 /
  `disconnected` → 記録のみ。トークン認証・E2Eテスト緑（Tasker不使用の現構成では
  この申告経路は使われないが手動テスト用として残存）
- [x] **ping監視主体へ設計変更（2026-09-10 ユーザー判断: Tasker不使用）**:
  yocron `light-api-check`（1分毎・no_overlap）の check.php に ON エッジ判定を追加。
  **ON = ping不通5分超過後に再び到達（ping_reconnect エッジ=帰宅）**。
  手動消灯保護のため light_state==="on" の間は ON しない（OFF判定が走って
  "off"になった後だけ復帰ONする）。**OFF = 切断申告5分 or ping不通5分**
  （従来どおり）。ONは最大1分遅れ（チェック間隔分）を許容
- [x] pixel_ip 設定（2026-09-10）: Pixel 10 をルータで 192.168.0.230 に DHCP予約
  （MAC 7A:F6-AA-50-E3-9E = SSIDごとのランダム化MAC・ARP実測と一致確認済み。
  ランダムMACはSSIDごとに固定なのでWi-Fi再加入等しない限り不変）。
  `data/config.json` の pixel_ip へ反映
- [x] `data/config.json` の `dry_run` → false 切替（2026-09-10・本番稼働開始）
- [x] 初回実地テスト（2026-09-10 20:11）: 外出→帰宅（Wi-Fi再接続）で
  **自動ON成功** — `auto_on why=ping_reconnect+1021s dry_run=0 result=ok`・
  照明点灯。ただし初回は yocron の env_path に /sbin が無く `/sbin/ping` が
  見つからない（exit 127=常時ping fail）状態だったため、check.php を
  **/sbin/ping フルパス固定**に修正して解決（手動実行はPATHが通っており
  気付かなかった・yocron 実行環境のPATHは env_path 依存という教訓）
- [ ] 外出5分での自動OFF確認（次の外出時に events.log で auto_off を確認）
- [ ] 状態同期の確認: 照明状態 = 「Pixel 10 が家 Wi-Fi に繋がっているか」に一致すること
  （一貫性・自己回復: スマホ再起動→再接続で勝手に点き直す）

### ~~スマホ側 Tasker 設定~~（不採用・2026-09-10）

- [x] Tasker での Wi-Fi Connected/Disconnected プロファイル → HTTP POST 方式は
  不採用（ユーザー判断）。connected 申告エンドポイントは light-api に残存

### フェーズ4: 運用観察

- [ ] スマホ電源 OFF・機内モード・再起動での挙動（消えた後、入れたら点き直るか）
- [ ] 夜間にイベントが一切起きないこと（家にいる間は Wi-Fi 切れないため）
- [ ] 照明のつけっぱなしゼロの確認（外出 5 分で自動消灯）
- [ ] 誤消灯が実際に困った場面が出たら緩和策を検討（深夜帯 OFF 保留・消灯前 ntfy 予告等。
  先回りして実装しない）

## 保留

### 判断済み・採用しない（2026-09-10 の設計判断）

- [x] GPSジオフェンス（tapo/SwitchBot/自作）— 不採用。要件は Wi-Fi 接続/切断エッジで満たる。
  「家に入る前から点けておきたい」が出てきたら自作アプリに GPS 層を足す程度で拡張可
- [x] 部屋センサー（毫米波 LD2410・入口 PIR・磁気ドアセンサー）— 不要。要件が
  「帰宅 ON・外出 OFF」に確定したため部屋の入退室検知は消えた。既存の
  promicro-presence 超音波も本自動化には使わない
- [x] OFF エッジ・クールダウン / 遅延確定（入口センサー議論の残滓）— Wi-Fi エッジ方式では
  「手動で消して退室→センサー反応→点く」の問題自体が存在しなくなったため不要
- [x] tapo ハブ IR を API から直叩き — pytapo（カメラ/電球/プラグ対象）も公式 Tapo Cloud API
  も現状 IR 非対応。新 H110 と API 整備の動向はウォッチし、整ったらゼロ円化へ乗換可
- [x] Google Home 音声経由の自動化 — Google Home は自発話を無視し外部からコマンドを
  注入する公式 API もないため不可。音声は人間の手動操作のまま（tapo 経由を維持）

### 照明リモコンが非 NEC だった場合の代替経線（フェーズ0の結果次第）

- [ ] servo_usb の学習パスを RAW 保存へ拡張（受信は生波形で取れており RAW 照合も既にある
  ので、保存先を NEC 形式から RAW に変えるだけで対応できる可能性）
- [ ] SwitchBot ハブミニ追加（〜3,000円・公式 Open API で IR カスタムコマンド送信・最短確実）
- [ ] 照明の IR 操作は tapo ハブ＋音声の手動のままにして自動化のみ諦め

## 概要

- **経緯**: tapo のジオフェンシングは要件（自宅近くで ON・500m 離れたら OFF）は完璧だが
  不発が多いので運用中止。「仕様どおり発動する」ことを自作で実現する。GPS でなく
  **Wi-Fi 接続/切断イベント**を素子にすれば位置推定と Play Services のジオフェンス配信に
  依存せず原理的に安定する（一般ユーザーには「tapo が坏れている」に見える現象の技術的真相）
- **構成**:
  `Pixel 10 --(Wi-Fi接続/切断・HTTP POST)--> Mac mini httpd(受付API)`
  `--> light_on.sh / light_off.sh --(USB 9600baud)--> servo_usb (HIDEF1)`
  `--> SEND_LIGHT_ON/SEND_LIGHT_OFF --> IR LED --> 照明`
- **性質**: 状態同期設計（スマホが Wi-Fi に繋がっている=ON/切れている状態が5分続く=OFF）。
  誤爆モードは「スマホの電源 OFF で照明が消える」のみで、復旧は音声 1 秒・電源 ON で
  自己回復。変な中間状態が残らない。副作用で照明のつけっぱなしが構造的にゼロになる
- **制約・ルール**: servo_usb は常用固定デバイス（`/dev/cu.usbmodemHIDEF1`・9600baud 固定）。
  他プロジェクトの実験目的の書込は禁止（実験は開発共通ポート `/dev/cu.usbmodem212101` か
  別ボード）。本プロジェクトの開発書込はエージェントが実行可（手順は AGENTS.md）。
  IRremote は v3.9.0 固定
  （v4.x は API 非互換・バージョンガードあり）。コード変更後は必ず `bash compile.sh`
- **関連**: fastrec ワークスペースでの検討から発展（2026-09-10）。Mac mini 側の受けの
  資産は `~/.zcode/MAC_SETUP.md` 参照
